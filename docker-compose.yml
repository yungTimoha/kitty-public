version: '3.8'

services:
  web:
    build: .
    container_name: kittygram_web
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - sqlite_data:/app/data

volumes:
  sqlite_data:
